{
  "name": "tycho",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Walking-skeleton engine: drive a rover over real Moon and Mars terrain with the real signal delay.",
  "scripts": {
    "lint": "node scripts/check_syntax.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "verify:boot": "node scripts/verify_boot.mjs",
    "gate": "npm run lint && npm run test:unit && npm run verify:boot"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tarang-tj/tycho.git"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  },
  "license": "MIT"
}
