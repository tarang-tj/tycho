"""Per-body site processors for the TYCHO terrain pipeline.

Split out of tools/site_pipeline.py (U4: keep files under ~250 lines) once
a fourth+ body was added. tools/site_pipeline.py re-exports everything here
for backward compatibility; tools/build_terrain.py's CLI is unchanged.
"""
